{
  "name": "holdem",
  "private": true,
  "version": "0.1.0",
  "description": "Texas Hold'em for friends \u2014 server-authoritative multiplayer NLHE on Cloudflare Workers + Durable Objects",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "test": "vitest run",
    "dev": "npm run dev --workspace=server & npm run dev --workspace=client & wait",
    "build": "npm run build --workspace=client",
    "typecheck": "tsc --noEmit -p shared && tsc --noEmit -p server && tsc --noEmit -p client && tsc --noEmit -p scripts",
    "bots": "vite-node scripts/bots.ts --",
    "assert:domain": "node scripts/assert-custom-domain.mjs",
    "assert:first-visit": "node scripts/assert-first-visit.mjs",
    "build:check": "npm run build && node scripts/assert-first-visit.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "playwright-core": "^1.62.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
