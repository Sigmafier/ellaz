{
  "name": "studio",
  "private": true,
  "version": "0.1.0",
  "description": "The Ellaz studio art bible - style glossary, code-authored sprites, engine-neutral exports, and the gallery to pick from. A sibling workspace: nothing here imports src/ and nothing in src/ imports this.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "gallery": "vite gallery --port 5188 --strictPort",
    "gallery:build": "vite build gallery",
    "export": "node export/export-all.mjs",
    "shots": "node scripts/render-gallery-shots.mjs",
    "render:styles": "node scripts/render-styles.mjs",
    "render:clips": "node scripts/render-clips.mjs",
    "render:techniques": "node scripts/render-techniques.mjs",
    "assert:boundary": "node scripts/assert-boundary.mjs",
    "assert:recipes": "node scripts/assert-recipe-headings.mjs",
    "assert:render": "node scripts/assert-render-smoke.mjs",
    "assert:manifest": "node scripts/assert-manifest-schema.mjs",
    "assert:atlas": "node scripts/assert-atlas-coverage.mjs",
    "assert:grammar": "node scripts/assert-frame-grammar.mjs",
    "assert:all": "npm run assert:boundary && npm run assert:tokens && npm run assert:recipes && npm run assert:render && npm run assert:manifest && npm run assert:atlas && npm run assert:grammar",
    "assert:controls": "node scripts/assert-boundary.mjs --control && node scripts/assert-tokens-parity.mjs --control && node scripts/assert-recipe-headings.mjs --control && node scripts/assert-render-smoke.mjs --control && node scripts/assert-manifest-schema.mjs --control && node scripts/assert-atlas-coverage.mjs --control && node scripts/assert-frame-grammar.mjs --control",
    "build:check": "npm run typecheck && npm test && npm run gallery:build && npm run export && npm run assert:all && npm run assert:controls",
    "gallery:preview": "vite preview gallery --port 5188 --strictPort",
    "assert:tokens": "node scripts/assert-tokens-parity.mjs"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^22.7.4",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "@vitejs/plugin-react": "^5.2.0",
    "class-variance-authority": "^0.7.1",
    "cn": "^0.2.5",
    "lucide-react": "^1.41.0",
    "playwright-core": "^1.62.1",
    "radix-ui": "^1.6.7",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "shadcn": "^4.21.0",
    "tailwindcss": "^4.3.3",
    "tw-animate-css": "^1.4.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  }
}
